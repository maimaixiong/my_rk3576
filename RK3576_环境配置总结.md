# RK3576 目标板环境配置与硬件档案

> 更新时间：2026-08-30
> 目标板：MYD-LR3576（米尔科技，基于 Rockchip RK3576）
> 本文档为后续开发工作的基础，所有连接方式、环境配置、已知问题均以本文为准。

---

## 一、连接方式（免密登录）

```bash
ssh rk3576              # 等价于 ssh myir@192.168.2.100
scp file rk3576:~/      # 文件传输同样免密
```

- 用户：`myir` / 密码 `myir`（sudo 密码相同）
- 别名配置在 mac `~/.ssh/config`：
  ```
  Host rk3576
      HostName 192.168.2.100
      User myir
      IdentityFile ~/.ssh/id_ed25519
      IdentitiesOnly yes
  ```
- 公钥：mac `~/.ssh/id_ed25519.pub` 已安装到板子 `~/.ssh/authorized_keys`

---

## 二、硬件档案

| 项目 | 规格 |
|---|---|
| 板卡 | MYIR Board MYD-LR3576（compatible: rockchip,rk3576-evb1-v10） |
| 系统 | Ubuntu 22.04.5 LTS (aarch64)，内核 6.1.75（2025-12-10 编译） |
| CPU | 8 核异构：4×Cortex-A72 + 4×Cortex-A53 |
| GPU | Mali-G52 MC3，动态 300~900MHz（当前空闲 300MHz） |
| NPU | rockchip,rk3576-rknpu，950MHz，6 TOPS |
| 内存 | 4GB LPDDR4/4X @ 2.112GHz（实测 725Mi 空闲占用） |
| 存储 | 32GB eMMC：p6 根分区 28.9G（已用 57%），实测读 288MB/s / 写 146MB/s |
| 显示 | DSI 屏已连接（1200×1920 竖屏），DP/HDMI 未接 |
| 音频 | ES8388 板载 codec (card0) + DP (card1) + HDMI (card2) |
| 视频 | MPP 硬编解（H264/H265/VP9/AV1 等），`/dev/mpp_service`、`/dev/video-dec0`、`/dev/video-enc0` |
| 摄像头 | rkcif + rkisp 双管线，27 个 video 节点（可接 MIPI CSI） |
| 网络 | 当前用 USB 100M 网卡 `enxec9a0c1a72b4`；板载 eth0/eth1（GMAC）未启用；双 CAN（can0/can1） |
| 串口 | ttyS1/S3/S4/S5/S6/S8 共 6 路 |
| 看门狗 | /dev/watchdog0 |
| LED | 3 个（mmc0 / run / user） |
| 温度 | 空闲 51~53°C（6 个热区：soc/bigcore/little/ddr/npu/gpu） |
| 固件链 | ddr-v1.05 → bl31-v1.14 → bl32-v1.05 → uboot 2025-12-10 |

---

## 三、网络与代理架构（重要）

```
RK3576 (192.168.2.100)  ──USB 网卡直连──>  mac (192.168.2.1)  ──en0 WiFi/有线──>  互联网
        │                                                        │
        └── 默认路由 via 192.168.2.1 ────────────────────────────┘
```

### 关键点
- **板子出网全部走 TCP 代理**（mac 未开 IP 转发，UDP 不通——这是架构选择，不是故障）
- 代理：`~/bin/simple_proxy.py`（mac 端纯 Python CONNECT 代理），监听 **0.0.0.0:1081**
- 代理由 **launchd 管理**（`com.ding.simpleproxy`）：RunAtLoad + KeepAlive，mac 开机自动运行、崩溃自动重启
- 域名解析发生在 mac 侧（板子把域名交给代理，代理负责解析）
- **板子本地 DNS 直连不通是正常的**，不影响走代理的程序

### 板子网络配置（已持久化，NetworkManager）
```
IP:       192.168.2.100/24 (manual, autoconnect=yes)
Gateway:  192.168.2.1
DNS:      /etc/resolv.conf → 223.5.5.5, 114.114.114.114
```

### 代理固化清单（板子端）
| 位置 | 内容 |
|---|---|
| `/etc/apt/apt.conf.d/99proxy` | apt 走 `http://192.168.2.1:1081` |
| `/etc/gitconfig` | git http/https proxy + `sslVerify=false` |
| `/etc/profile.d/proxy.sh` | 环境变量 `http_proxy` / `https_proxy`（注释可随时关闭） |

> git 注意：走代理时需 `git -c http.sslVerify=false` 或已通过系统配置关闭证书验证（板子缺 CA 证书包）。

---

## 四、时间同步（RTC 故障临时方案）

### 背景
- 板载 RTC **hym8563 @ i2c-2 0x51 无 ACK**（`i2cdetect` 确认），系统时间曾错乱 6 个月
- RTC 为**硬件级故障**（芯片损坏/虚焊/电池缺失），软件无法修复

### 方案：开机 HTTP 时间同步
- 脚本：`/usr/local/bin/sync-time.sh` —— 通过代理 curl `mirrors.ustc.edu.cn` 的 Date 响应头，`date -s` 校准（自动处理 GMT→CST）
- 服务：`synctime.service`（oneshot，开机自动执行，已验证 SUCCESS）
- 若以后修复 RTC 硬件，可 `systemctl disable synctime` 停用

---

## 五、MPP 硬解工具链（已修复）

### 背景
- 板载用户态 MPP 库（2021-05）与内核 MPP 驱动（2024-07）版本不匹配 → 旧 `mpi_dec_test` 初始化挂起
- 已从 GitHub 克隆最新源码（2026-08-26）在板子本地编译并安装

### 现状
| 项 | 值 |
|---|---|
| 新版库 | `/usr/local/lib/librockchip_mpp.so.0`（ldconfig 优先加载，已验证 `mpp version: 0986d01`） |
| 新版工具 | `/tmp/mpp/build/test/mpi_dec_test`（未装到 /usr/bin，如需可 make install 或拷贝） |
| 源码 | `/tmp/mpp/`（已克隆，可 `git pull` 更新） |

### 硬解实测
```
H.264 1080p：60 帧全部解码成功，test success，峰值内存 24.7MB
输出 YUV 186,624,000 字节 = 60 × 1920×1080×1.5 ✓ 数据校验正确
```

### 使用方法
```bash
# MP4 需先转 Annex-B 裸流（MPP 解码器只认裸流）
ffmpeg -y -i test.mp4 -c:v copy -bsf:v h264_mp4toannexb -an test.h264
# 硬解测试（-t 7=H264, 8=H265）
sudo mpi_dec_test -i test.h264 -t 7 -n 60 -o out.yuv
```

---

## 六、NPU（已验证可用）

| 项 | 值 |
|---|---|
| 运行时 | RKNN 2.3.0 + 驱动 0.9.7，librknnrt.so |
| 实测 | mobilenet_v1 INT8：**2.4ms/帧 ≈ 420 FPS**，Top1 93.5% 正确 |
| 官方模型 | `/usr/share/model/RK3576/mobilenet_v1.rknn`（另有 RK3562/3566/3588 版本） |
| 测试命令 | `sudo rknn_common_test /usr/share/model/RK3576/mobilenet_v1.rknn /usr/share/model/dog_224x224.jpg 3` |
| 服务 | `rknn_server` 已在后台运行 |
| 注意 | 板上无 python rknnlite（pypi 无此包，GitHub releases 无 wheel），**NPU 开发走 C API**（`/usr/include/rknn_api.h` + `/usr/lib/librknnrt.so`，板上 gcc 可本地编译） |

### 摄像头实时推理（已完成并验证）

**硬件链路**：OS04C10 sensor（i2c-5 @0x36）→ CSI2-DPHY0 → rkcif → rkisp → `/dev/video11`（NV12 2688×1520，mainpath）

**运行结果**：450 帧 / 15s = **30.0 FPS 满速**，推理 3-6ms/帧，识别到场景物体（crate/forklift）

**程序**：`/Users/ding/work/rk3576/rknn_cam.c`（本地源码，已 scp 到板 `/tmp/rknn_cam.c`，含软件 AE）
```bash
# 编译
sudo gcc -O2 -o /tmp/rknn_cam /tmp/rknn_cam.c -lrknnrt -I/usr/include -pthread -lm
# 运行（3 个可选参数：模型 / 标签 / 秒数；秒数=0 无限运行）
sudo /tmp/rknn_cam /usr/share/model/RK3576/mobilenet_v1.rknn /usr/share/model/imagenet_classes.txt 0
```

**一站式服务**（开机自启，含自动曝光+推理）：`camera-npu.service`
```bash
systemctl status camera-npu     # 查看状态
journalctl -u camera-npu -f     # 实时日志（AE 亮度 + Top1）
```

**已知限制与替代方案**：
- ~~板载 rkaiq 无 `os04c10` iqfile → 无法自动 3A~~ → **已解决（软件 AE 方案）**，详见下方「自动 3A 专题」
- ffmpeg 4.4 不支持 ISP multiplanar 节点取流，用 `v4l2-ctl --stream-mmap` 或 gst 或自定义 C 程序
- 曝光调节：`sudo v4l2-ctl -d /dev/v4l-subdev3 -c exposure=1566 -c analogue_gain=800`（exposure 2-1566, gain 128-1984）

### 自动 3A 专题（2026-08-30 排查结论）

**排查过程**：
1. rkaiq 3A 原本完全无法启动（`rkaiq engine prepare failed`）→ 发现是 iqfile 缺失
2. 官方 iqfile 格式为 JSON（isp39），命名规则 `{sensor}_{module}_{lens}.json`，无 module/lens 信息时用 `NC`（如 `os04c10_NC_NC.json`）
3. 全网搜索（GitHub SDK/radxa/Rockchip releases）**均无 os04c10 官方 iqfile**（os04c10 是 commaai 定制 sensor，Rockchip 官方未提供）
4. 用同系列 os04a10（同为 4MP 2688×1520）iqfile 改名替代 → 3A 框架正常启动（ISP 各模块 opMode:auto 工作），**但 AE 不写 sensor 曝光**
5. 对照实验（imx415 iqfile 同样不工作）→ 排除 iqfile 参数问题
6. **根因**：设备树拓扑为 sensor → rkcif → sditf → rkisp（桥接模式），sensor 实体挂在 media0（rkcif），而 rkaiq 因 media0 无 rkisp 实体而跳过 media0 → **找不到 sensor → AE 无曝光回写通道**

**结论**：rkaiq 的 AE 无法驱动 sensor，除非改设备树（sensor 直连 rkisp，需重刷 boot，风险高）或从 MYIR 官方获取完整 BSP。

**替代方案（已实现）— 软件 AE（已集成进 rknn_cam）**：
- 程序：`/Users/ding/work/rk3576/soft_ae.c`（独立版，可单独用）；**rknn_cam 已内置 AE 闭环**（一体化）
- 原理：V4L2 常驻取流 → 每 5 帧采样亮度 → 闭环调 sensor exposure/gain（优先 exposure 后 gain，目标亮度 50 可调）
- 实测：亮度 35→50 自动收敛；抗干扰（注入错误曝光 6 秒内自动纠正）；推理 30FPS 同时 AE 正常
- 注意：rkaiq_3A_server 仍在运行（ISP 去噪/色彩等模块正常工作），rknn_cam 的 AE 只接管曝光闭环，两者互补不冲突

**若需真 3A（含 AWB 自动白平衡）的路径**：
1. 联系 MYIR 获取官方 SDK（板卡厂商，有账号下载）或 Rockchip FAE 要 os04c10 iqfile
2. 或改设备树 sensor 直连 rkisp（需要 DTS 源码 + 重刷 boot 分区）

### 硬件编解码链路要点
| 设备 | 说明 |
|---|---|
| /dev/video0-9 | rkcif raw（Bayer） |
| /dev/video10+ | rkisp 输出（video11=mainpath NV12 2688×1520） |
| /dev/v4l-subdev3 | OS04C10 sensor 控件 |
| /dev/mpp_service, video-dec0/enc0 | 硬编解码 |

---

## 七、系统状态速查

```bash
# 常用诊断
ssh rk3576
aplay -l                      # 音频设备
cat /sys/class/thermal/thermal_zone*/temp   # 温度(毫度)
cat /sys/class/devfreq/27800000.gpu/cur_freq # GPU频率
cat /sys/class/devfreq/*npu*/cur_freq        # NPU频率
nmcli con show enxec9a0c1a72b4 | grep ipv4  # 网络配置
systemctl status synctime synctime.service  # 时间同步服务
```

---

## 八、已知问题与后续建议

| 问题 | 状态 | 建议 |
|---|---|---|
| RTC hym8563 无应答 | ⚠️ 硬件故障 | 补焊/更换芯片/接 VBAT 电池；修复后停用 synctime |
| 出网依赖 mac 代理 | 正常架构 | mac 开机即自动运行；如需板子独立上网可接路由器（改网关/DNS 即可） |
| 板载 eth0/eth1 未启用 | 待用 | 有需要时 `nmcli` 配置，可换掉 USB 网卡提速（GMAC 支持千兆） |
| DNS 直连不通 | 正常架构 | 走代理程序不受影响 |
| 系统更新 | 正常 | `apt update` 已验证可用（30.3MB 拉取成功） |
| 编解码 SDK | 已就绪 | ffmpeg 为 4.4 官方版（无 rkmpp），如需 ffmpeg 硬解可后续装 rockchip ffmpeg 或写 V4L2 管道 |

---

## 十、远端图像传输（WebRTC，参考 commaai/teleoprtc 架构）✅

### 架构
```
RK3576 (video11 NV12 2688×1520 + ES8388 麦克风)         浏览器 / mac aiortc 客户端
  ├─ V4L2 直读 (v4l2_cap.py, ctypes, 30fps)              ├─ WebRTC 视频 (H264)
  │    └─ cv2 缩放 1280×720 → I420 (含软件AWB/AE)          └─ WebRTC 音频 (Opus)
  │         └─ MPP H264 硬编码 (mpph264enc, 30fps能力)       └─ http://192.168.2.100:8080
  └─ arecord 子进程 48kHz 立体声 → Opus
       └─ aiortc 编码（预编码 H264 包直发）
            └─ RTCPeerConnection + WS 信令 (:8081)
```

### 使用
```bash
# 浏览器访问（打开即自动连接并播放实时视频，无需任何依赖）
http://192.168.2.100:8080

# mac 命令行接收测试（首次需装依赖）
cd /Users/ding/work/rk3576/webrtc && ./mac_setup.sh   # 一键安装 aiortc+websockets
python3 mac_receiver.py 192.168.2.100 20
# 若 python3 是 conda/pyenv 等环境：PYTHON=<该环境python> ./mac_setup.sh
```

### 文件与部署
| 文件 | 位置 | 作用 |
|---|---|---|
| web_rtc_aiortc.py | 板 `/usr/local/bin/webrtc/`（本地 `webrtc/`） | 推流服务（aiortc + 信令 + 页面） |
| v4l2_cap.py | 同目录 | V4L2 MPLANE 直读（ctypes，30fps） |
| index.html | 同目录 | 浏览器端页面（自动连接播放） |
| mac_receiver.py | 本地 `webrtc/` | mac 端命令行接收测试 |
| web-rtc.service | systemd（active + 开机自启） | 服务管理 |

### 实测性能
- **1280×720 @ 17fps**（H264 硬编码，MPP 编码能力 30fps，端到端受 sender/网卡限制）
- **音频**：48kHz 立体声 Opus，20ms/帧稳定推流
- 单帧端到端延迟 <100ms（局域网）
- 图像处理：软件 AWB（灰度世界）+ 软件 AE（anti-flicker 50Hz）+ NV12 UV 解包修正

### 调试踩坑记录（重要）
1. **gst v4l2src 只有 10fps**（RK ISP mainpath 的 poll 问题）→ 改 V4L2 直读（ctypes）→ 30fps
2. **vertical_blanking 被 rkaiq 改成 3202**（os04a10 iqfile 冒充的副作用）→ 帧率降到 10fps → 已停用 rkaiq_3A + `isp-fix.service` 固化 blanking=54
3. **aiortc 非 trickle ICE**：候选内嵌 SDP（`pc.localDescription.sdp` 而非 `offer.sdp`），默认 Google STUN 会卡（局域网用 `iceServers=[]`）
4. **VideoFrame 构造**：用 `VideoFrame(width,h,format="yuv420p")` + planes 填充（from_ndarray 的 I420 在 PyAV17 不支持）
5. **recv 空转循环会 GIL 饿死 asyncio**：用 `run_in_executor(q.get)` 阻塞取帧

### 可调参数
- 分辨率：`OUT_W/OUT_H`（web_rtc_aiortc.py，默认 640×360）
- 码率：offer SDP 的 `b=AS:1500`（1.5Mbps）
- 端口：HTTP 8080 / WS 8081

### 与 teleoprtc 的对应关系
| teleoprtc | 本实现 |
|---|---|
| VideoTrack（摄像头帧→编码） | CameraTrack（V4L2→缩放→VP8） |
| Peer/Client（RTCPeerConnection） | RTCPeerConnection + WS 信令 |
| comma connect 信令服务 | 自建 websockets 信令 |

---

## 十一、鱼眼去畸变 + YOLO 检测（RKNN NPU）✅

### 鱼眼转平面
- 检测到鱼眼：中心 (1267,807) 半径 1193（圆形鱼眼）
- cv2.fisheye 去畸变：预计算 remap 表（Y 720p + UV 分离），运行时 cv2.remap（9.4ms/帧）
- 参数可调：`FISHEYE` 配置（fx/fy/cx/cy/k1-k4），在 web_rtc_aiortc.py 顶部

### YOLO 检测（VisDrone 11 类，NPU 加速）
- 模型：`yolov8s_airborne.rknn`（RK3576 预编译，FP16 640×640）
- 类目：pedestrian/people/bicycle/car/van/truck/tricycle/awning-tricycle/bus/motor/others
- 推理：ctypes 直调 librknnrt（`rknn_api_ctypes.py`，无需 rknnlite/torch）
- 处理：letterbox + RGB + DFL 后处理 + NMS（`yolo_detect.py`）
- 集成：检测线程每 5 帧一次（实测 243-350ms/次），结果 WS 推送 → 浏览器 canvas 画框

### 关键经验（RK3576 NPU 模型获取）
1. RKNN 转换需要真 torch（aarch64 wheel 需 ARMv8.2，RK3576 是 ARMv8.0 → Illegal instruction；**torch 1.13 老版本兼容 ARMv8.0**，可正常转换）
2. 转换替代：**找预编译 rknn**（GitHub 搜 RK3576 项目，如 inteintegrity/Seeed 系）
3. 注意平台兼容：RK3588 的 rknn 不能跑 RK3576（硬件版本校验）
4. RKNN 推理免 rknnlite：ctypes 调 librknnrt（rknn_init/inputs_set/run/outputs_get）
5. YOLO 颜色通道：模型训练用 RGB，cv2 读的是 BGR，需转换（否则置信度极低）

### 最终性能调优参数（2026-09）
| 参数 | 值 | 说明 |
|---|---|---|
| 曝光上限 | 486 行（≈10ms） | 50Hz 半周期防条纹 + 帧率翻倍（972 行会拖帧率） |
| 鱼眼 fov_scale | 0.48 | 输出视野（<1 视野更大） |
| 鱼眼 Y/UV remap | LINEAR / NEAREST | 画质与速度平衡 |
| YOLO 检测频率 | 每 15 帧 | 降低 GIL 竞争 |
| 帧率 | 17-23fps（720p） | exposure=486 时 23.8fps，服务完整 17.3fps |
| 端到端延迟 | ~120ms（板端）+ 浏览器 jitter | 延迟大头在浏览器解码缓冲 |

---

## 十二、环境速览（一张图）

```
免密ssh → 硬件档案 → 网络(代理) → 时间同步 → MPP硬解 → NPU推理
   ✓          ✓           ✓           ✓          ✓         ✓
```

后续在此基础上的开发方向：
1. 音频播放/录音应用（ES8388）
2. 视频硬解显示（MPP + DRM/KMS）
3. NPU 视觉推理（RKNN C API，接 MIPI 摄像头）
4. CAN 通信
5. 触摸屏/显示应用（DSI 1200×1920）
